# Landing — group 1

Target: the current patch-careers-ui landing, not the published legacy site.

Approved:
1. Scroll-driven before/after with pronounced parallax.
2. Three job requirements connected to relevant experience through scroll.
3. Role-specific tailoring CTA. Current CTA goes to registration; this is disclosed. No job input is collected by this demo, so there is no submitted job to discard.
4. Keep the current hero headline; clarify one profile adapted to different roles in the supporting copy.
5. Two review variants: `?variant=a` combines the evidence into 3 sections; `?variant=b` retains 6. Both alternate evidence with product content, preserve sources and the AI chart, and use the green navbar over green sections. Default is A for preview, not a final user selection.
6. No scores in the hero.
7. Same underlying experience adapted to two roles, keeping the facts intact.

Deferred explicitly by the user:
8. Guest analysis / deciding what is available before registration. Do not implement or promise real analysis without an account in this phase. Revisit separately.

Groups 2 and 3 are outside this implementation.

Desktop demonstrations use three scroll beats; a subsequent gesture advances the section. On mobile, the animation follows the reading position within taller content. Reduced motion renders the complete demonstration without parallax.
