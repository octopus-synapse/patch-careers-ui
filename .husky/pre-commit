#!/bin/sh
# V2 pre-commit gate — checks declared in .precommit.yaml (lint via Biome, Nx
# typecheck, architectural boundary specs), run in parallel by the shared
# @octopus-synapse/precommit runner — same tooling as profile-services.
pnpm exec octopus-run-parallel --config .precommit.yaml
