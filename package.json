{
  "name": "patch-careers-ui",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run check",
    "test": "turbo run test",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "turbo": "^2"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
