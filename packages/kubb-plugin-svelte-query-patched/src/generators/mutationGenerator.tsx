// Fork delta: upstream `mutationGenerator` is unchanged — re-exported
// from the published `@kubb/plugin-svelte-query` to keep the fork
// surface area minimal.
export { mutationGenerator } from '@kubb/plugin-svelte-query/generators';
