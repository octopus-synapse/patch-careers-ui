// Fork delta: upstream `Mutation` component is unchanged — re-exported
// from the published `@kubb/plugin-svelte-query` to keep the fork
// surface area minimal. PD-010 only affects path-param-bearing queries.
export { Mutation } from '@kubb/plugin-svelte-query/components';
