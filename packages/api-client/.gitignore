# Kubb output (ts/, zod/, hooks/) — regenerated by `pnpm sdk:generate` once
# the first client-swagger.json lands here. Not committed: the contract
# source in `contracts/` is the durable artefact.
src/generated/ts/
src/generated/zod/
src/generated/hooks/

# tsc artefacts
dist/
*.tsbuildinfo
