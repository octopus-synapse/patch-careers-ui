/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * ProFile API
 * ## ProFile - Resume & Portfolio Management API

ProFile is a comprehensive platform for creating, managing, and exporting professional resumes and portfolios.

### Features
- **Authentication**: JWT-based authentication with email verification and password reset
- **Resume Management**: Full CRUD operations for resumes with multiple sections
- **Rich Content Sections**: Education, Experience, Skills, Projects, Certifications, and more
- **Export Options**: Generate PDF and DOCX documents from resumes
- **File Upload**: Upload profile pictures and attachments via S3-compatible storage
- **User Onboarding**: Guided onboarding flow for new users

### Authentication
All protected endpoints require a valid JWT token in the Authorization header:
```
Authorization: Bearer <your-jwt-token>
```

### Rate Limiting
- Authentication endpoints: 5 requests per minute
- General API: 100 requests per minute

### Error Responses
All errors follow a consistent format:
```json
{
  "statusCode": 400,
  "message": "Error description",
  "error": "Bad Request"
}
```
 * OpenAPI spec version: 1.0.0
 */
import * as zod from 'zod';


/**
 * Returns `{ locale, entries: { [CODE]: localized message } }`. Locale is chosen from `Accept-Language` (supports `en`, `pt-BR`).
 * @summary Error-message dictionary in the negotiated locale
 */
export const I18nGetErrorsResponse = zod.object({
  "locale": zod.enum(['en', 'pt-BR']).describe('Locale the strings are in (negotiated from Accept-Language).'),
  "entries": zod.record(zod.string(), zod.string()).describe('Map from stable error code to the localized message.')
})

/**
 * Returns `{ locale, entries: { [EnumName]: { [VALUE]: label } } }`. Locale is chosen from `Accept-Language` (supports `en`, `pt-BR`).
 * @summary Prisma enum label dictionary in the negotiated locale
 */
export const I18nGetEnumsResponse = zod.object({
  "locale": zod.enum(['en', 'pt-BR']).describe('Locale the strings are in (negotiated from Accept-Language).'),
  "entries": zod.record(zod.string(), zod.record(zod.string(), zod.string())).describe('Map from Prisma enum name to its `{ value → label }` map in the negotiated locale.')
})

/**
 * Returns `{ locale, entries: { [TYPE]: { title, body, params[] } } }`. Templates may contain `{param}` placeholders — the client substitutes them at render time using the `params` list as the allowed key set.
 * @summary Notification-template dictionary in the negotiated locale
 */
export const I18nGetNotificationsResponse = zod.object({
  "locale": zod.enum(['en', 'pt-BR']).describe('Locale the strings are in (negotiated from Accept-Language).'),
  "entries": zod.record(zod.string(), zod.object({
  "title": zod.string().describe('Short title for UI surfaces like the bell dropdown.'),
  "body": zod.string().describe('Longer body for the notifications page \/ email.'),
  "params": zod.array(zod.string()).describe('Placeholder names that must be supplied when rendering.')
})).describe('Map from NotificationType value to its template in the negotiated locale.')
})

