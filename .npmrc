# pnpm settings
auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false

# @octopus-synapse/* is published to GitHub Packages; token comes from the environment
@octopus-synapse:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
