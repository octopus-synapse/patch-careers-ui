import { authMenuIconPalette } from "@patch-careers/tokens";
import { NAV_CONTROL_SIZE } from "./nav-bar.contract";

/** Measurements and colors shared by the web and native /auth menu control. */
export const AUTH_NAV_SIDE_INSET = 24;
export const AUTH_MENU_SIZE = NAV_CONTROL_SIZE;
export const AUTH_MENU_RADIUS = 17;
export const AUTH_MENU_ICON_SIZE = 28;
export const AUTH_MENU_ICON_COLOR = authMenuIconPalette;
export const AUTH_MENU_STROKE_WIDTH = 2.3;
