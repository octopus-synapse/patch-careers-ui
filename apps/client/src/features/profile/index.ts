/**
 * Profile feature — public API. The authenticated user's public-profile
 * preview screen. Import only from "@/features/profile".
 */
export { ProfileScreen } from "./components/profile-screen";
