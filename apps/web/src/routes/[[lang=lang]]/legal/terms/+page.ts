// Legal pages are indexable + linked from the cookie banner / footer.
// Override the layout's `ssr = false` so crawlers (and screen readers
// before JS executes) see the full content.
export const ssr = true;
